import x from "../../static/login.jpg"

const Leftside = () => {
    return (
        <img style={{ width: '100%', minHeight: '100%' }} src={x} alt='Left of login' className="rounded-end"/>
    );
};

export default Leftside;